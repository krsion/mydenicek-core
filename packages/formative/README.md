# Formative examples

Executable formative scenarios for `@mydenicek/core`.

These tests exercise the public core API from a separate workspace package so the examples do not live inside `packages/core`.

## Development

From the repository root:

```sh
deno test --allow-all --no-check packages/formative/tests
```
